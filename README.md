# Broadcast

This version uses local JSON storage instead of MongoDB.

## Run

1. Install dependencies with `npm install`
2. Create a `.env` file with your bot token settings
3. Start the app with `npm start`

Data is stored automatically in `data/storage.json` at runtime.
